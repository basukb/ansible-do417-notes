- name: testing roles
  hosts: win2.example.com
  gather_facts: false
  roles:
    - { role: simple, value1: FIRST }
    - role: simple
    - { role: simple, value1: SECOND }
  tasks:
    - name: after roles
      debug:
        msg: this will only execute after all roles {{ value1 }}

    - name: import roles
      import_role:
        name: simple
      vars:
        value1: asdf